<div class="container">
  <div class="jumbotron mt-4">
    {{#each Object}}
    <p>{{this.dataValues.id}}</p>
      <span role="img" aria-label="Face with rolling eyes">🙄</span>
    {{/each}}
    <a href="/">← Back To Home</a>
  </div>
</div>